#!/usr/bin/env node
//#region @backend
var fs = require('fs')
var path = require('path')
var pathToDistRun = path.join(__dirname, '../dist/start.js');
var pathToBundletRun = path.join(__dirname, '../start.js');

if (fs.existsSync(pathToDistRun)) {
  var run = require(pathToDistRun.replace(/\.js$/g, '')).run;
} else {
  var run = require(pathToBundletRun.replace(/\.js$/g, '')).run;
}
run(process.argv);
//#endregion
